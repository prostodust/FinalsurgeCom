package pages;

import org.openqa.selenium.WebDriver;

public class UpdateWorkoutPage extends HomePage {

    /**
     * Сreating an object
     *
     * @param driver
     */
    public UpdateWorkoutPage(WebDriver driver) {
        super(driver);
    }
}
