package pages;

import org.openqa.selenium.WebDriver;

public class WorkoutDetailsAddPage extends HomePage {

    /**
     * Сreating an object
     *
     * @param driver
     */
    public WorkoutDetailsAddPage(WebDriver driver) {
        super(driver);
    }
}
